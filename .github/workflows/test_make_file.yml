name: Test Making a File
on: push

jobs:
  run:
    name: Add a text file
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - run: echo "123" > fileTwo.txt
      - uses: EndBug/add-and-commit@v9
        with:
          message: 'Add the very useful text file'
          add: '*.txt --force'